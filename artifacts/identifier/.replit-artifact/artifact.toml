kind = "web"
previewPath = "/"
title = "Identifly"
version = "1.0.0"
id = "artifacts/identifier"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24487

[services.development]
run = "pnpm --filter @workspace/identifier run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/identifier", "run", "build" ]
publicDir = "artifacts/identifier/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24487"
BASE_PATH = "/"
